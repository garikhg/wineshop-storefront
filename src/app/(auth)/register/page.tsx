"use client";import React from 'react';import NextLink from "next/link";import {Container, Logo} from "@/components";import {Input} from "@/components/ui/input";import {Button} from "@/components/ui/button";import {settings as SETTINGS} from "@/confing/settings";import {useRouter} from "next/navigation";const Register = () => {    const router = useRouter();    return (        <Container className="flex flex-col items-center lg:py-28 h-screen" size="small">            <div className="w-full max-w-md flex flex-col items-stretch mx-auto gap-8 bg-light-green rounded-lg p-6">                <div className="text-center">                    <div className="w-full relative flex items-center justify-center mb-8">                        <NextLink href="/">                            <Logo/>                        </NextLink>                    </div>                    <h2 className="scroll-m-20 pb-2 text-2xl font-bold tracking-tight first:mt-0">                        Sign Up                    </h2>                    <p className="text-sm">Create a new account</p>                </div>                <div className="flex flex-col gap-6">                    <div>                        <Input type="email" placeholder="Email"/>                    </div>                    <div>                        <Input type="password" placeholder="Password"/>                    </div>                    <div>                        <Input type="password" placeholder="Confirm password"/>                    </div>                    <div className="flex items-stretch">                        <Button className="w-full">Sign Up</Button>                    </div>                    <div className="flex items-center justify-center">                        <Button                            variant="link"                            onClick={() => router.push('/sign-in')}                        >                            Sign In to Account                        </Button>                    </div>                </div>            </div>            <p className="text-sm text-slate-900 mt-8">                {SETTINGS.copyright}            </p>        </Container>    );};export default Register;