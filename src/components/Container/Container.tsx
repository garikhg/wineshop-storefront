import React, {ReactNode} from 'react';import {cn} from "@/lib/utils";import {cva, type VariantProps} from "class-variance-authority";const containerVariants = cva(    "w-full px-6 mx-auto max-w-screen-2xl", // Default styles    {        variants: {            size: {                small: "max-w-screen-sm", // 640px                medium: "max-w-screen-md", // 768px;                large: "max-w-screen-lg", // 1024px;                xl: "max-w-screen-xl", // 1280px;                "2xl": "max-w-screen-2xl", // 1536px;            }        },        defaultVariants: {            size: "2xl",        }    });interface ContainerProps extends VariantProps<typeof containerVariants> {    children: ReactNode,    className?: string,}const Container: React.FC<ContainerProps> = ({children, className, size}) => {    return <div className={cn(containerVariants({size}), className)}>{children}</div>;};export default Container;