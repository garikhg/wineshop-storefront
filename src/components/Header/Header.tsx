import React from "react";import {Navbar} from "@/components";const Header = () => {    return (        <header            className="sticky top-0 z-50 w-full py-4 xl:py-2 border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60"            role="banner"        >            <Navbar/>        </header>    );};export default Header;